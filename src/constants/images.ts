import dashboard from '../assets/dashboard.png';
import help from '../assets/help.png';

interface ImageFiles {
  dashboard: any;
  help: any;
}

const Images: ImageFiles = { dashboard, help };

export default Images;
